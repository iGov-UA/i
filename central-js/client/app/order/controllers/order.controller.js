angular.module('order', []).controller('OrderController', function($scope, $state, $location, $window, MessagesService, UserService) {
    //if ($state.is('index.order')) {
    //    return $state.go('index.order.search');
    //}
});
